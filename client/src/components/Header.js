
export default function Header() {
    return (
        <div style={{textAlign:"start"}}>
           <h1 style={{color:"#333333",fontFamily:"Helvetica",fontSize:60, fontWeight:600, margin:0}}>Never miss out on a meeting again.</h1>
           
           <h2 style={{fontSize:40, fontWeight:200, letterSpacing:0}}>Upload your recording, catch  up in minutes.</h2>
        </div>
    );
  }